#!/bin/bash
set -x

echo $PATH

breakdancer-max
samtools --help
bcftools --version
bedtools --version
bowtie2 --version
bwa
gatk
freebayes
picard -h
sniffles --version
minimap2 --version
dysgu
hisat2 --version
kallisto version
macs2 --version
meme -version
tomtom -version
bedGraphToBigWig
fetchChromSizes
assembly-stats -v
canu -version
jellyfish --version
seqtk
velveth
velvetg
wtdbg2 -V
genomescope2 --version
