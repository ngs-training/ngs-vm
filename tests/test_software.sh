#!/bin/bash

echo $PATH

breakdancer-max

samtools --help
bcftools --version
bedtools --version
bowtie2 --version
bwa --help
gatk4
freebayes
igv &

picard -h

sniffles --version
minimap2 --version
dysgu

hisat2 --version
kallisto version
sleuth.R

macs2 --version
meme -version
tomtom -version
bedGraphToBigWig
fetchChromSizes
r-ngsplot

assembly-stats -v
canu -version
jellyfish --version
seqtk
velveth
velvetg
wtdbg2 -V
genomescope2 --version
